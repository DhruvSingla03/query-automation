# Environment Configuration Template
# Copy to /etc/onboarding/onboarding.env and fill in values

# Vault authentication
VAULT_ADDR=https://vault.company.com
VAULT_ROLE_ID=<onboarding-service-role-id>
VAULT_SECRET_ID=<onboarding-service-secret-id>

# Product database Vault paths
VAULT_PATH_FASTAG_ACQ=secret/data/fastag/database
VAULT_PATH_AUDIT=secret/data/audit/database

# Environment
ENV=development  # Set to 'production' in prod
